app: # subscription-aggregator-service 1.0
  log:
    enabled: true
    file_path: "application.log"
    keep_files: true # Whether to keep old log files
  api:
    host: "0.0.0.0"
    port: 8080
    base_path: "/api/v1"
    gin_release_mode: true
  database:
    host: "localhost"
    port: 5432
    user: "username"
    password: "password"
    database_name: "subscription-aggregator-service"
    ssl_mode: "disable"
